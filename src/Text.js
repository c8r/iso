import styled from 'styled-components'
import { Box } from 'grid-styled'
import {
  fontWeight,
  textAlign,
} from 'styled-system'

const Text = styled(Box)([],
  fontWeight,
  textAlign
)

export default Text
